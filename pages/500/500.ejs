<!doctype html>
<html>
<head><%- bootstrap %></head>
<body>
  <h1>500, Internal server error</h1>

  <p><% if (message) { %><%= message %><% } %></p>

  <% if (env !== 'production' && stack) { %>
  <pre><%= stack %></pre>
  <% } %>

  <div data-pagelet="diagnostics"></div>
</body>
</html>
